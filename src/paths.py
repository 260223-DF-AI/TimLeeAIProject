from pathlib import Path

SRC_ROOT = Path(__file__).resolve().parent
API_ROOT = SRC_ROOT / "api"
DATABASE_ROOT = SRC_ROOT / "database"
MODEL_ROOT = SRC_ROOT / "models"
UTILS_ROOT = SRC_ROOT / "utils"
